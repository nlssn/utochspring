---
pagination:
  data: collections.eventCities
  size: 1
  alias: city
permalink: /stad/{{ city.slug }}/
layout: "layouts/default.njk"
eleventyComputed:
  title: "Lopp i närheten av {{ city.title }}"
---

<h1>Alla lopp i {{ city.title }}</h1>

{% set posts = collections.events | include('data.cities', city.title) %} 
<ul>
  {% for post in posts %}
  <li><a href="{{ post.url }}">{{ post.data.title }}</a></li>
  {% endfor %}
</ul>